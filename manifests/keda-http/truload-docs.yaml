---
# KEDA HTTP scale-to-zero for truload-docs. First request wakes the pod,
# subsequent requests go straight through.
apiVersion: http.keda.sh/v1alpha1
kind: HTTPScaledObject
metadata:
  name: truload-docs
  namespace: truload
  labels:
    app: truload-docs
spec:
  hosts:
    - kuraweigh-docs.masterspace.co.ke
  scaleTargetRef:
    name: truload-docs
    kind: Deployment
    apiVersion: apps/v1
    service: truload-docs
    port: 80
  replicas:
    min: 0
    max: 2
  scalingMetric:
    requestRate:
      granularity: "1s"
      targetValue: 50
      window: "1m"
---
apiVersion: v1
kind: Service
metadata:
  name: keda-http-interceptor
  namespace: truload
  labels:
    app: keda-http-bridge
spec:
  type: ExternalName
  externalName: keda-add-ons-http-interceptor-proxy.infra.svc.cluster.local
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: truload-docs
  namespace: truload
  labels:
    app: truload-docs
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "90"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "90"
spec:
  ingressClassName: nginx
  rules:
    - host: kuraweigh-docs.masterspace.co.ke
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: keda-http-interceptor
                port:
                  number: 8080
  tls:
    - hosts:
        - kuraweigh-docs.masterspace.co.ke
      secretName: truload-docs-tls
