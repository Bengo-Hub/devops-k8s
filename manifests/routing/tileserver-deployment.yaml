apiVersion: apps/v1
kind: Deployment
metadata:
  name: tileserver
  namespace: logistics
  labels:
    app: tileserver
    component: map-tiles
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: tileserver
  template:
    metadata:
      labels:
        app: tileserver
        component: map-tiles
    spec:
      # Generate Kenya tiles on first run if not present
      initContainers:
<<<<<<< Updated upstream
        # Generate Kenya MBTiles on first run via Planetiler
        - name: generate-tiles
=======
        - name: init-tiles
>>>>>>> Stashed changes
          image: ghcr.io/onthegomap/planetiler:latest
          command:
            - /bin/sh
            - -c
            - |
              set -e
              MBTILES="/data/tiles/kenya.mbtiles"
              mkdir -p /data/tiles

              if [ -f "$MBTILES" ] && [ -s "$MBTILES" ]; then
<<<<<<< Updated upstream
                echo "Tiles already exist at $MBTILES ($(du -h $MBTILES | cut -f1)), skipping."
                exit 0
              fi

              # Remove any empty/corrupted mbtiles file
              rm -f "$MBTILES"

              echo "Generating Kenya vector tiles from Geofabrik OSM extract..."
              echo "This may take 10-30 minutes depending on resources."
              java -cp @/app/jib-classpath-file com.onthegomap.planetiler.Main \
                --area=kenya \
                --download \
                --download-threads=2 \
                --output="$MBTILES" \
                --force \
                --nodemap-type=array \
                --storage=ram \
                2>&1

              if [ -f "$MBTILES" ] && [ -s "$MBTILES" ]; then
                echo "Tile generation complete: $(du -h $MBTILES | cut -f1)"
              else
                echo "ERROR: Tile generation failed."
                exit 1
              fi
          volumeMounts:
            - name: tileserver-data
              mountPath: /data
=======
                SIZE=$(stat -c%s "$MBTILES" 2>/dev/null || echo 0)
                if [ "$SIZE" -gt 1000000 ]; then
                  echo "Kenya tiles exist ($(du -h $MBTILES | cut -f1)), skipping generation."
                  exit 0
                fi
              fi

              echo "First run — generating Kenya vector tiles with Planetiler..."
              java -jar /app/planetiler.jar \
                --area=kenya \
                --download \
                --download-threads=2 \
                --output="$MBTILES" \
                --force \
                --nodemap-type=array \
                --storage=ram \
                2>&1

              echo "Tile generation complete: $(du -h $MBTILES | cut -f1)"
>>>>>>> Stashed changes
          resources:
            requests:
              cpu: "1"
              memory: 3Gi
            limits:
              cpu: "4"
              memory: 6Gi
<<<<<<< Updated upstream
=======
          volumeMounts:
            - name: tileserver-tiles
              mountPath: /data/tiles
>>>>>>> Stashed changes
      containers:
        - name: tileserver
          image: maptiler/tileserver-gl:latest
          args:
            - --config
<<<<<<< Updated upstream
            - /data/config.json
=======
            - /config/config.json
>>>>>>> Stashed changes
            - --verbose
            - --port
            - "8080"
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          volumeMounts:
<<<<<<< Updated upstream
            - name: tileserver-data
              mountPath: /data
            - name: tileserver-config
              mountPath: /data/config.json
              subPath: config.json
=======
            - name: tileserver-tiles
              mountPath: /data/tiles
              readOnly: true
            - name: tileserver-config
              mountPath: /config
              readOnly: true
>>>>>>> Stashed changes
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
      volumes:
        - name: tileserver-tiles
          persistentVolumeClaim:
<<<<<<< Updated upstream
            claimName: tileserver-data
=======
            claimName: tileserver-tiles
>>>>>>> Stashed changes
        - name: tileserver-config
          configMap:
            name: tileserver-config
