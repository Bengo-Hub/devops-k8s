apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tileserver-tiles
  namespace: logistics
  labels:
    app: tileserver
    component: map-tiles
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
