apiVersion: v1
kind: Secret
metadata:
  name: argocd-notifications-secret
  namespace: argocd
type: Opaque
stringData:
  # Provide your actual webhook URL and token in your cluster's secret management.
  # For now, placeholders allow Argo CD to render objects without errors.
  webhook-url: "https://example-webhook.local/argocd"
  token: "replace-me"
  # Email/SMTP placeholders
  email-smtp-host: "smtp.gmail.com"
  email-smtp-port: "587"
  email-username: "<REPLACE_WITH_SMTP_EMAIL>"
  email-password: "<REPLACE_WITH_SMTP_APP_PASSWORD>"
  email-from: "Bengo ERP <noreply@bengoerp.com>"
  email-starttls: "true"
  # Default recipient for templates
  email-to: "codevertexitsolutions@gmail.com"


