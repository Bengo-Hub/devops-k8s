App Manifests
-------------

Per-app ArgoCD Applications live here. Each app uses the shared chart under `charts/app` and has its own `values.yaml` file for image and runtime configuration.

Added:
- `auth-service/` – application and values for the Auth Service.

