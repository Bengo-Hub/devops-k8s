apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: calcom
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Bengo-Hub/devops-k8s.git
    targetRevision: main
    path: apps/calcom/manifests
  destination:
    server: https://kubernetes.default.svc
    namespace: calcom
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
