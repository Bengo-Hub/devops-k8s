apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keda-http
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://kedacore.github.io/charts
    chart: keda-add-ons-http
    targetRevision: 0.10.0
    helm:
      values: |
        # Override broken gcr.io/kubebuilder/kube-rbac-proxy default
        # (v0.13.0 was removed when kubebuilder migrated to quay.io).
        images:
          kubeRbacProxy:
            repository: quay.io/brancz/kube-rbac-proxy
            tag: v0.14.4
        # Single replica each on a single-node cluster — chart default
        # of 3 interceptor + 3 external-scaler replicas burned ~1.5 CPU
        # for nothing.
        interceptor:
          replicas:
            min: 1
            max: 1
          # Default conditionWaitTimeout is 20s — too short for Next.js
          # cold starts (~30-60s). Bumped to 120s so the first request
          # successfully waits for scale-up instead of 502-ing.
          # responseHeaderTimeout 500ms default is also too tight for
          # SSR responses.
          keda:
            conditionWaitTimeout: "120s"
            responseHeaderTimeout: "30s"
        scaler:
          replicas: 1
  destination:
    server: https://kubernetes.default.svc
    namespace: infra
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: apiextensions.k8s.io
      kind: CustomResourceDefinition
      jsonPointers:
        - /metadata/annotations
