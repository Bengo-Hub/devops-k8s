apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: subscriptions-ui
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Bengo-Hub/devops-k8s.git
    targetRevision: main
    path: charts/app
    helm:
      valueFiles:
        - ../../apps/subscriptions-ui/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: subscriptions
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
