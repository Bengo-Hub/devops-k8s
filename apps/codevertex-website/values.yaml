# Codevertex Website — Production Helm Values
# Next.js 16 marketing & academy site
# Namespace: codevertex | Domain: codevertexitsolutions.com
nameOverride: codevertex-website
fullnameOverride: codevertex-website
# ============================================================================
# IMAGE CONFIGURATION
# NOTE: image.tag is updated by build.sh only — never edit manually
# ============================================================================
image:
  repository: docker.io/codevertex/codevertex-website
  tag: "61237b21"
  pullPolicy: Always
  pullSecrets:
    - name: registry-credentials
# ============================================================================
# REPLICA AND SCALING CONFIGURATION
# ============================================================================
replicaCount: 1
autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 2
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80
# ============================================================================
# SERVICE CONFIGURATION
# ============================================================================
service:
  type: ClusterIP
  port: 80
  targetPort: 3000
# ============================================================================
# DEPLOYMENT STRATEGY
# ============================================================================
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 1
    maxUnavailable: 0
minReadySeconds: 15
# ============================================================================
# ENVIRONMENT VARIABLES
# Sensitive vars come from K8s secret (codevertex-website-secrets):
#   DATABASE_URL        — pgbouncer URL (?pgbouncer=true) for Next.js runtime
#   DIRECT_DATABASE_URL — direct postgres URL for Prisma migrations (entrypoint.sh)
#   ANTHROPIC_API_KEY   — Vera AI chatbot
# NEXT_PUBLIC_* are baked at Docker build time — listed here for documentation only.
# ============================================================================
envFromSecret: codevertex-website-secrets
env:
  # Treasury integration
  - name: NEXT_PUBLIC_TREASURY_TENANT
    value: codevertex
  - name: NEXT_PUBLIC_TREASURY_PAY_URL
    value: https://books.codevertexitsolutions.com/pay
  # SSO link
  - name: NEXT_PUBLIC_SSO_URL
    value: https://accounts.codevertexitsolutions.com
  # Application URL
  - name: NEXT_PUBLIC_APP_URL
    value: https://codevertexitsolutions.com
  # Notifications service (S2S email) — port 4000
  - name: NOTIFICATIONS_API_URL
    value: http://notifications-api.notifications.svc.cluster.local:4000
  # NATS JetStream for domain event publishing (digitika_consumer handles emails when set)
  - name: EVENTS_NATS_URL
    value: nats://nats.messaging.svc.cluster.local:4222
  # MarketFlow AI proxy — Vera chatbot server-side calls
  - name: MARKETFLOW_AI_URL
    value: https://marketflowai.codevertexitsolutions.com
  # Node environment
  - name: NODE_ENV
    value: production
# ============================================================================
# HEALTH CHECKS
# GET /healthz returns 200 {"status":"ok"}
# ============================================================================
healthCheck:
  enabled: true
  readiness:
    httpGet:
      path: /healthz
      port: http
    # Allow up to 3 min for prisma db push + seed + Next.js cold start
    initialDelaySeconds: 60
    periodSeconds: 10
    timeoutSeconds: 10
    failureThreshold: 18
  liveness:
    httpGet:
      path: /healthz
      port: http
    initialDelaySeconds: 120
    periodSeconds: 20
    timeoutSeconds: 10
    failureThreshold: 5
# ============================================================================
# RESOURCE LIMITS AND REQUESTS
# Prisma CLI + engine + Next.js runtime needs at least 512Mi
# ============================================================================
resources:
  requests:
    cpu: 50m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 768Mi
# ============================================================================
# INGRESS CONFIGURATION
# ============================================================================
ingress:
  enabled: true
  ingressClassName: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # Cache static assets at ingress level
    nginx.ingress.kubernetes.io/proxy-buffering: "on"
  hosts:
    - host: codevertexitsolutions.com
      paths:
        - path: /
          pathType: Prefix
    - host: www.codevertexitsolutions.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - hosts:
        - codevertexitsolutions.com
        - www.codevertexitsolutions.com
      secretName: codevertex-website-tls
# ============================================================================
# MONITORING
# ============================================================================
monitoring:
  enabled: false
serviceMonitor:
  enabled: false
# ============================================================================
# VPA — Recommendation-only mode (observe before enforcing)
# ============================================================================
verticalPodAutoscaling:
  enabled: true
  updateMode: "Off"
  minCPU: 50m
  maxCPU: 500m
  minMemory: 128Mi
  maxMemory: 1Gi
  controlledResources: ["cpu", "memory"]
  controlledValues: RequestsAndLimits
  recommendationMode: true
pdb:
  enabled: true
  minAvailable: 1
