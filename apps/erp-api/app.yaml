apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: erp-api
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Bengo-Hub/devops-k8s.git
    targetRevision: main
    path: charts/app
    helm:
      # Use external values file (allows build.sh to update image tag)
      valueFiles:
        - ../../apps/erp-api/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: erp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
      # PruneLast ensures old resources are only deleted after new ones are healthy
      - PruneLast=true
      # ApplyOutOfSyncOnly prevents unnecessary resource updates
      - ApplyOutOfSyncOnly=true
      # ServerSideApply for better conflict resolution
      - ServerSideApply=true
      # Remove Replace=true - it forces replacement of immutable resources (PVCs) which fails
      # RespectIgnoreDifferences tells ArgoCD to honor the ignoreDifferences config below
  ignoreDifferences:
      # Application-level ignore for immutable PVC fields
      - group: ""
        kind: PersistentVolumeClaim
        jsonPointers:
          - /spec/volumeName
          - /spec/volumeMode
          - /status
