apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: projects-ui
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Bengo-Hub/devops-k8s.git
    targetRevision: main
    path: charts/app
    helm:
      valueFiles:
        - ../../apps/projects-ui/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: projects
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true

